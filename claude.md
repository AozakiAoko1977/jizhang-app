# 黑马记账 - 产品文档

---

## 一、项目概述

**项目名称**：黑马记账

**项目简介**：一款桌面端个人记账应用，帮助用户记录和管理日常开销（人民币），支持两级分类的收支管理，提供消费统计与数据分析功能。

**目标用户**：有个人/家庭记账需求的普通用户

**开发起始日期**：2026-07-19

---

## 二、目标平台

| 平台 | 支持 |
|------|------|
| Windows 10/11 | ✅ |
| macOS (Apple Silicon & Intel) | ✅ |

---

## 三、技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 桌面框架 | **Electron** | 将 Web 技术打包为跨平台桌面应用 |
| 前端框架 | **React** + TypeScript | 构建用户界面 |
| UI 组件库 | **Ant Design** | 成熟的企业级 React 组件库，中文生态好 |
| 数据库 | **SQLite** (better-sqlite3) | 轻量级本地数据库，数据存储在用户电脑 |
| 构建工具 | **Vite** | 快速的前端构建工具 |
| 打包工具 | **electron-builder** | 打包为 Windows (.exe) 和 Mac (.dmg) 安装包 |

---

## 四、记账分类体系

### 两级分类（一级大类 → 二级小类）

| 一级大类 | 二级小类 |
|---------|---------|
| 🍜 餐饮 | 三餐、零食、饮品、外卖、聚餐 |
| 🚗 交通 | 公交地铁、打车、加油、停车、高铁/机票 |
| 🛒 购物 | 日用品、服饰、数码、家居、美妆 |
| 🏠 住房 | 房租、房贷、物业、水电燃气、维修 |
| 🎮 娱乐 | 电影、游戏、旅游、运动健身、KTV/酒吧 |
| 💊 医疗 | 看病、药品、体检、牙科 |
| 📚 教育 | 课程、书籍、考试报名、文具 |
| 🎁 人情 | 送礼、红包、婚礼/生日、孝敬父母 |
| 📱 通讯 | 话费、宽带、快递 |
| 📌 其他 | 无法分类 |

---

## 五、核心功能

### 5.1 记账功能
- 记录每笔支出：**金额（人民币 ¥）、一级分类、二级分类、日期、备注**
- 支持新增、编辑、删除账单
- 显示账单列表，按日期倒序排列

### 5.2 分类管理
- 预设上述两级分类（用户开箱即用）
- 支持用户自定义添加/编辑/删除分类

### 5.3 统计与分析
- 按月查看总支出
- 按大类查看支出占比（饼图/柱状图）
- 按小类查看消费明细
- 日期范围筛选

### 5.4 数据管理
- 数据存储在本地 SQLite 数据库
- 支持导出数据（CSV 格式）
- 支持数据备份与恢复

---

## 六、重要开发规则 ⚠️

> **此规则贯穿整个项目开发周期，必须严格遵守：**

用户不懂编程技术，因此在项目开发过程中：

1. **所有技术相关的决定**（包括但不限于：技术选型、库的选择、架构设计、功能实现方式、UI 交互方案、性能优化方案等），AI 必须先列出 **多个可行方案**，用通俗易懂的语言解释每个方案的 **优势和劣势**，然后由用户来做出最终选择。

2. **禁止行为**：AI 不得在没有征求用户意见的情况下，自行做出技术决策。即使是"最佳实践"或"推荐方案"，也必须先列出选项并等待用户确认。

3. **沟通规范**：
   - 避免使用过多技术术语，或用简单比喻解释
   - 方案选择通常提供 2-3 个选项，以表格形式呈现优劣
   - 给出明确推荐（标注 ⭐推荐），但最终决定权在用户

---

## 七、项目文件结构

```
记账app/
├── claude.md                    # 产品文档（本文件）
├── package.json                 # 项目配置与依赖
├── electron-vite.config.ts      # electron-vite 构建配置
├── tsconfig.json                # TypeScript 配置（渲染进程）
├── tsconfig.node.json           # TypeScript 配置（主进程）
├── electron-builder.yml         # 打包配置
├── scripts/
│   └── run.js                   # 启动脚本（处理环境变量兼容）
├── src/
│   ├── main/                    # Electron 主进程
│   │   ├── index.ts            # 主进程入口
│   │   └── database.ts         # SQLite 数据库操作
│   ├── preload/
│   │   └── index.ts            # 预加载脚本（桥接主进程与渲染进程）
│   └── renderer/                # React 前端
│       ├── index.html           # HTML 入口
│       └── src/
│           ├── main.tsx          # React 入口
│           ├── App.tsx           # 根组件（路由配置）
│           ├── App.css           # 全局样式
│           ├── components/
│           │   └── AppLayout.tsx # 侧边栏布局
│           ├── pages/
│           │   ├── Home.tsx      # 首页/概览
│           │   ├── Record.tsx    # 记账页
│           │   ├── Category.tsx  # 分类管理
│           │   ├── Statistics.tsx# 统计分析
│           │   └── Settings.tsx  # 设置
│           └── types/
│               └── electron.d.ts # TypeScript 类型声明
├── out/                         # 构建输出（自动生成）
├── node_modules/                # 依赖包（自动生成）
└── resources/                   # 应用图标等静态资源
```

---

## 八、开发路线图

### 整体规划

整个开发分为 **6 个阶段**，按优先级从基础搭建到最终打包，逐步推进。

```
阶段 1          阶段 2          阶段 3          阶段 4          阶段 5          阶段 6
基础搭建  ──▶  记账核心  ──▶  分类管理  ──▶  统计报表  ──▶  数据管理  ──▶  打磨打包
(骨架)        (能记账)       (管分类)       (看分析)       (导数据)       (发成品)
```

---

### 阶段 1：项目基础搭建 🏗️

> **目标**：把项目"骨架"搭建起来，能在电脑上看到一个能启动的窗口

| 任务 | 说明 |
|------|------|
| 初始化项目 | 创建 Electron + React + TypeScript 项目 |
| 配置 UI 库 | 引入 Ant Design，设置中文 |
| 搭建数据库 | 连接 SQLite，创建账单和分类的数据表 |
| 应用框架 | 设置侧边栏导航 + 页面路由（记账 / 分类 / 统计 / 设置） |

**阶段完成标志**：双击启动应用，能看到带导航的窗口界面

---

### 阶段 2：记账核心功能 ✏️

> **目标**：能够记录、查看、修改、删除账单

| 任务 | 说明 |
|------|------|
| 记账表单 | 输入金额 → 选大类 → 选小类 → 选日期 → 填备注 |
| 账单列表 | 按日期倒序显示所有账单 |
| 编辑账单 | 点击某条账单可修改其内容 |
| 删除账单 | 删除某条账单（需确认提示） |
| 今日概览 | 首页显示今日支出合计 |

**阶段完成标志**：完整走通"新增 → 查看 → 修改 → 删除"一条龙

---

### 阶段 3：分类管理 📂

> **目标**：用户可以按自己的习惯管理分类

| 任务 | 说明 |
|------|------|
| 预设分类 | 首次启动自动导入 10 个大类 + 对应小类 |
| 查看分类 | 树形或分组展示两级分类 |
| 新增分类 | 用户可添加自己的大类/小类 |
| 编辑分类 | 修改分类名称或所属关系 |
| 删除分类 | 删除不再需要的分类（已有账单的分类不可删除） |

**阶段完成标志**：分类体系完整可用，用户可自由增删改

---

### 阶段 4：统计与分析 📊

> **目标**：直观看到"钱花在哪了"

| 任务 | 说明 |
|------|------|
| 月度概览 | 显示当月总支出、日均支出、记账天数 |
| 大类饼图 | 按一级分类展示支出占比 |
| 大类柱状图 | 按一级分类对比各月支出 |
| 小类明细 | 点击某个大类，深入查看小类消费排行 |
| 日期筛选 | 支持自定义时间段查看统计 |

**阶段完成标志**：图表完整展示，能清晰回答"我这个月钱花哪了"

---

### 阶段 5：数据管理 💾

> **目标**：数据安全可控，能导出能备份

| 任务 | 说明 |
|------|------|
| CSV 导出 | 将账单数据导出为 Excel 可打开的 CSV 文件 |
| 数据备份 | 一键备份整个数据库到指定位置 |
| 数据恢复 | 从备份文件恢复数据 |
| 数据清空 | 清除所有数据（需二次确认） |

**阶段完成标志**：数据导入导出功能正常，备份恢复链路通畅

---

### 阶段 6：打磨与打包 🎁

> **目标**：把应用变成可以安装使用的成品

| 任务 | 说明 |
|------|------|
| UI 美化 | 统一配色、间距、字体，提升视觉体验 |
| 异常处理 | 输入校验、错误提示、空状态引导 |
| 应用图标 | 设计黑马记账 Logo |
| Windows 打包 | 生成 .exe 安装包 |
| macOS 打包 | 生成 .dmg 安装包 |
| 基础测试 | 在 Windows 和 Mac 上分别测试核心功能 |

**阶段完成标志**：可在两台电脑上正常安装使用

---

### 路线图总览

```
阶段 1 ████████████████████████████████  ✅ 已完成
阶段 2 ████████████████████████████████  ✅ 已完成
阶段 3 ████████████████████████████████  ✅ 已完成
阶段 4 ████████████████████████████████  ✅ 已完成
阶段 5 ████████████████████████████████  ✅ 已完成
阶段 6 ████████████████████████████████  ✅ 已完成
─────────────────────────────────────
全部 6 个阶段已完成 ✅
```

---

## 十、运行与打包

### 开发运行
```bash
npm run dev      # 启动开发模式（热更新）
```

### 生产构建
```bash
npm run build    # 构建生产版本到 out/ 目录
```

### Windows 打包
```bash
npm run pack:win # 生成 ZIP 包到 release/ 目录
```
> 输出文件：`release/黑马记账-1.0.0-win.zip`（解压后运行 `黑马记账.exe`）

### macOS 打包（需在 Mac 上执行）
```bash
npm run pack:mac # 生成 DMG 安装包
```

### 应用图标
- Windows 图标：将 `icon.ico` 放入 `resources/` 目录
- macOS 图标：将 `icon.icns` 放入 `resources/` 目录
- 当前使用 Electron 默认图标，可在 `electron-builder.yml` 中配置

---

## 九、待确认事项

以下事项将在后续开发中逐一确认：
- [ ] 是否需要收入记录功能（目前仅规划支出）
- [ ] 是否需要月度预算设置与提醒
- [ ] 是否需要多账户（现金、银行卡、微信、支付宝等）
- [ ] 应用图标设计
- [ ] 是否支持深色模式
